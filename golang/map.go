package main

func main() {
	/*DECLARATION
	var x map[string]int
	x := make(map[string]int)
	*/

	/*
		age := make(map[string]int)
		age[`rasel`] = 25

		fmt.Println(age["rasel"])   // prints 25
		fmt.Println(age["Hossain"]) // prints 0
	*/

	/*
		temp := make(map[string]string)
		fmt.Println(temp["rasel"]) // prints ''
	*/

	/*
		age := make(map[string]int)
		age["rasel"] = 25

		map calls return two values first value is the result and second value
		returns the boolean true or false wheather the lookup is successful or not

		num, isHit := age["rasel"]
		fmt.Println(num)
		fmt.Println(isHit) // boolean true/false
	*/

}
